services:
  dash-site:
    build: .
    container_name: dash-site
    restart: unless-stopped
    ports:
      - "3002:80"
    # REMOVE any "volumes:" section for now to ensure it uses the GitHub files
