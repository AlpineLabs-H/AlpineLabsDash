services:
  dash-site:
    image: nginx:latest
    container_name: dash-site
    restart: unless-stopped
    ports:
      - "3002:80"
    volumes:
      # Change the left side to the actual path on your server
      - /home/user/dash-site:/usr/share/nginx/html/logo.svg
